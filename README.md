Forked from a pure C implementation of LeNet5 (without some of the skip connections). For a class, we rewrote the forward pass to CUDA, did quantization, pruning, and are finishing getting this running on an MSP430FR5994.
